@model Damacana.Models.Purchase
@{
    ViewBag.Title = "AddPurchase";
}

<p style='font-size:35px; color:lawngreen'>AddPurchase</p>
@using (Html.BeginForm("SavePurchase", "Home", FormMethod.Post))
{ <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Confirm buy" class="btn btn-default" />

        </div>
    </div>}
